import * as styles from './NotfoundPage.module.scss';

const NotfoundPage: React.FC = () => (
  <div className={styles.container}>
    <div className={styles.info}>Страница не найдена</div>
  </div>
);

export default NotfoundPage;
